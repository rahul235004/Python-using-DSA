class Solution:
    def arraySortedOrNot(self, arr) -> bool:
        i=0
        for j in range(1,len(arr)):
                if(arr[i]>arr[j]):
                    return False
                else:
                    i+=1
        return True         
